import {InjectionToken} from '@angular/core';

/**
 * An injection token for the components that created by using {@link SidenavProjectionService} that will be used in DI container
 */
export const SIDENAV_PROJECTION_ITEM_DATA = new InjectionToken('sidenav-projection-data-item');
